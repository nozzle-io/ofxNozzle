name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-macos:
    runs-on: macos-14
    steps:
      - name: Setup openFrameworks
        uses: nandenjin/openframeworks-action@v1
        with:
          command: setup-of
          of_version: "0.12.0"

      - uses: actions/checkout@v4
        with:
          path: openFrameworks/addons/ofxNozzle
          submodules: recursive

      - name: Create test app
        run: |
          mkdir -p openFrameworks/apps/devApps/ofxNozzleTest/src
          cat > openFrameworks/apps/devApps/ofxNozzleTest/src/main.cpp << 'EOF'
          #include "ofMain.h"
          #include "ofApp.h"
          int main() {
            ofSetupOpenGL(100, 100, OF_WINDOW);
            ofRunApp(new ofApp());
          }
          EOF
          cat > openFrameworks/apps/devApps/ofxNozzleTest/src/ofApp.h << 'EOF'
          #pragma once
          #include "ofMain.h"
          #include "ofxNozzle.h"
          class ofApp : public ofBaseApp {
          public:
            void setup() override;
            void draw() override;
          private:
            ofxNozzleSender sender;
            ofxNozzleReceiver receiver;
          };
          EOF
          cat > openFrameworks/apps/devApps/ofxNozzleTest/src/ofApp.cpp << 'EOF'
          #include "ofApp.h"
          void ofApp::setup() {}
          void ofApp::draw() {}
          EOF
          echo "ofxNozzle" > openFrameworks/apps/devApps/ofxNozzleTest/addons.make

      - name: Generate project
        uses: nandenjin/openframeworks-action@v1
        with:
          command: projectgenerator
          project_root: openFrameworks/apps/devApps/ofxNozzleTest

      - name: Build
        uses: nandenjin/openframeworks-action@v1
        with:
          command: build
          project_root: openFrameworks/apps/devApps/ofxNozzleTest

  build-windows:
    runs-on: windows-2022
    steps:
      - name: Setup openFrameworks
        uses: nandenjin/openframeworks-action@v1
        with:
          command: setup-of
          of_version: "0.12.0"

      - uses: actions/checkout@v4
        with:
          path: openFrameworks/addons/ofxNozzle
          submodules: recursive

      - name: Create test app
        shell: bash
        run: |
          mkdir -p openFrameworks/apps/devApps/ofxNozzleTest/src
          cat > openFrameworks/apps/devApps/ofxNozzleTest/src/main.cpp << 'EOF'
          #include "ofMain.h"
          #include "ofApp.h"
          int main() {
            ofSetupOpenGL(100, 100, OF_WINDOW);
            ofRunApp(new ofApp());
          }
          EOF
          cat > openFrameworks/apps/devApps/ofxNozzleTest/src/ofApp.h << 'EOF'
          #pragma once
          #include "ofMain.h"
          #include "ofxNozzle.h"
          class ofApp : public ofBaseApp {
          public:
            void setup() override;
            void draw() override;
          private:
            ofxNozzleSender sender;
            ofxNozzleReceiver receiver;
          };
          EOF
          cat > openFrameworks/apps/devApps/ofxNozzleTest/src/ofApp.cpp << 'EOF'
          #include "ofApp.h"
          void ofApp::setup() {}
          void ofApp::draw() {}
          EOF
          echo "ofxNozzle" > openFrameworks/apps/devApps/ofxNozzleTest/addons.make

      - name: Generate project
        uses: nandenjin/openframeworks-action@v1
        with:
          command: projectgenerator
          project_root: openFrameworks/apps/devApps/ofxNozzleTest

      - name: Build
        uses: nandenjin/openframeworks-action@v1
        with:
          command: build
          project_root: openFrameworks/apps/devApps/ofxNozzleTest

  build-linux:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y libdrm-dev libgbm-dev libegl-dev libgl-dev

      - name: Compile addon sources
        run: |
          FAIL=0
          for f in $(find src/platform/linux -name '*.cpp' 2>/dev/null); do
            echo "=== Compiling: $f ==="
            g++ -std=c++17 -fPIC -fno-exceptions -fno-rtti \
              -I libs/nozzle/include \
              -I libs/nozzle/libs/plog/include \
              -I src \
              -DNOZZLE_HAS_DMA_BUF \
              -c "$f" -o /dev/null || { echo "FAILED: $f"; FAIL=1; }
          done
          exit $FAIL
